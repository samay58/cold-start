# Repair How it wins generation and failure handling

Implement the reviewed specification at `/Users/samaydhawan/Projects/active/cold-start/docs/superpowers/specs/2026-09-14-how-it-wins-recovery.md`. Deliver a production repair and a verified extension experience. Preserve useful company research when optional analysis fails. Do not promise that external models can never fail.

Read `AGENTS.md`, `SECURITY.md`, `SPEC.md`, `docs/deployment.md`, the recovery spec, and `docs/qa/column-how-it-wins-diagnosis-2026-09-14.md`. Apply fable-judgment, fable-execution, fable-verification, say-less, and unslop. Check the current checkout, worktrees, remote main, production deployment, Neon database, Inngest app, and installed extension version. The diagnosis was made at `fe4493658ad647d393b4e489aeeff700c2105892`; verify changes since then.

Work on an isolated `codex/` repair branch. The spec, this prompt, and the diagnosis may initially be uncommitted documents in the original checkout; carry those three files into the repair deliberately. Preserve all unrelated changes. This execution request authorizes scoped implementation, necessary additive migrations, deployment, extension packaging, and publication of the reviewed repair to main. It does not authorize unrelated cleanup or provider changes.

The observed failure is specific: Column requested How it wins, but strategy entry 17 failed a strict union schema. Zod errors bypassed the existing correction branch. The worker recorded an application failure while Inngest showed a completed execution. No valid judgment was saved, and the extension hid the missing result. The original rejected entry was not retained. Do not invent its missing field or call a controlled fixture the original answer.

Follow the spec's design quality requirements. Keep one judgment contract, one recovery policy, and one durable source of job status. Reuse valid completed stages. Make correction survive worker restarts through bounded, short-lived private candidate storage, with no candidate content in logs or API responses. Avoid error-string matching, company-specific exceptions, fabricated defaults, and parallel retry loops. Preserve the normal successful experience without another click or a slower profile. Keep Inngest as the execution system; do not build a general workflow framework.

Implement the spec in reviewable parts:

1. Recover from supported output-shape and semantic errors without weakening evidence checks. Normalize only unambiguous variations. Share a two-request ceiling between transport recovery and correction. Send specific bounded feedback and prior-answer context. Keep all 80 strategies accounted for.
2. Persist the How it wins job, safe validation details, paid-call reservations, and every attempt's available usage independently of a successful judgment. Missing usage is unknown. Guard duplicate requests, uncertain dispatch, worker replay, stale evidence, cancellation, and interrupted execution. Paid attempts must fit inside Vercel's invocation limit and cancel their actual HTTP requests.
3. Add authenticated status and one bounded manual retry of How it wins alone. Preserve the saved profile and any still-valid read. Do not rerun synthesis or debit another analysis allowance. Make failure visible after reload. Do not confuse failed, unrequested, thin evidence, and no distinct advantage.
4. Verify, release, and check Column through the actual installed extension. A server deploy does not update an extension. Preserve the existing public/private boundary and client compatibility.

Start with the no-cost reproduction. The controlled diagnostic tests and logs are under `.cold-start/column-how-it-wins/`. Turn the missing correction into a failing regression, then prove it passes. Add real Postgres and browser checks for the job lifecycle, accounting, retry, and display. Replay frozen public evidence through the real adapter and persistence path. Test exhausted budgets, malformed second answers, stalled headers and bodies, rejected credentials, unsupported parameters, duplicate clicks, late responses, and crashes around paid-call persistence. Run the full repository gate and review the final diff.

For this execution request, the proposed fresh verification cap is $5 total, including diagnostics and production canaries. This cap becomes authorization only when I issue this prompt for execution; preparing the spec does not authorize spending. Keep this repair's ledger separate from earlier work. Before every paid request, reserve its conservative maximum using current official pricing and token limits. Unknown usage consumes the full reservation. Run paid checks serially. Stop before the next reservation would exceed the cap. Do not infer spending permission from account balances or enable automatic purchases. Complete free work even if paid verification cannot fit.

Read credentials only from the existing ignored project environment files. Never put them in prompts, commands, logs, screenshots, commits, or reports. Keep current model routing unless a verified capability blocks the repair; then present the specific finding before changing providers.

Before production changes, obtain a fresh review of the implementation and actual user journey, resolve blocking findings, record rollback targets, inspect active jobs, and test the additive migration and versioned step rollout. After release, verify the deployed revision, Inngest sync, job state, judgment when available, authenticated API, public redaction, costs, allowances, and actual extension behavior. Use the scoped legacy-Column retry described in the spec. Demonstrate successful correction locally and a valid analytical outcome for Column in production. A clearer failure message alone does not complete the repair. If an external condition prevents that check, report partial verification. If a canary exposes a repair regression, disable new retry admission and restore the prior compatible deployment. Preserve historical records.

Correct the existing local guide's claim that the extension already displays How it wins failures; update it to verified behavior and the released source revision. Keep its plain language and existing design.

Finish with a short report: what changed, which checks passed, Column's observed outcome, actual and reserved spend, deployed and extension revisions, and any remaining limit. If an external condition blocks completion, leave a precise continuation record. Do not claim success from mocks, a green Inngest badge, or a hidden error.
