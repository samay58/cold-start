# How it wins: finish the repair in production

Paste this whole file as the opening prompt of a fresh Claude Code session in `~/Projects/active/cold-start`.

---

You are finishing the "How it wins" repair in Cold Start. The repair itself shipped on 2026-08-25 and is deployed. Your job is to prove it in production, finish the two measurements it could not reach, and close the loose ends, without reopening settled decisions.

Read first: `CLAUDE.md`, `docs/qa/how-it-wins-repair-2026-08-25.md` (the repair record: what changed, the before-and-after numbers, the handoff list), `docs/superpowers/specs/2026-08-25-how-it-wins-production-repair-prompt.md` (the original brief and its twelve decisions, D1 to D12; they stand), and `~/.claude/CLAUDE.md`. The anti-slop kill list binds every string you write. No em dashes. Run `python3 ~/.claude/scripts/slopcheck.py <file>` on any prose file.

Do not re-derive what is below. It was verified on the evening of 2026-08-25 against the production database, the Vercel deployment list, and the batch artifacts. Verify anything you intend to contradict.

## What is true right now

- `origin/main` is at `ddfa1d9`, eight commits from the repair (`f40dfaa..ddfa1d9`). Full `npm run check` was green at the head, knip included. The branch `how-it-wins-repair` still exists on origin and in the worktree `~/Projects/active/cold-start-hiw`; that worktree is where the work happened and it holds the batch artifacts under `eval/curation/how-it-wins-batch/` (gitignored) plus symlinks to the corpus. The local `main` checkout in `~/Projects/active/cold-start` is behind origin and carries three uncommitted doc lines in `CLAUDE.md` and `AGENTS.md` (the `dev:inngest` and `db:local:status` command rows and a CI-deviations paragraph rewrite); they are older than the branch's edits to the same files and need to be re-applied by hand after a `git stash`, `git pull --ff-only`, then the stash dropped.
- Production is deployed from `ddfa1d9` with `HOW_IT_WINS_ENABLED=true`, `LLM_HOW_IT_WINS_JUDGE_MODEL=claude-opus-5`, `LLM_HOW_IT_WINS_MODEL=claude-opus-5`, the editor at its default `deepseek/deepseek-v4-pro`, and `HOW_IT_WINS_REFINEMENT` unset (on). Migration 0018 (`how_it_wins_judgments`) is applied on Neon; the table had zero rows at the end of the session. No analysis run had exercised the flag yet.
- The read runs in the Inngest function `how-it-wins-read` (`apps/web/src/inngest/how-it-wins-function.ts`, event `card/how-it-wins.requested`, steps load, judge, write, verify, store), dispatched by the analysis run after its store. The parent run's trace carries `howItWins.status` (`deferred`, then `read`, `nothing_stands_out`, `failed`, `stale`, `thin_file`, or `skipped`), `judgmentRef`, `judgeSummary`, and `losses`; the event `how-it-wins.complete` lands on the parent trail with `metadata.status`. Nobody has confirmed that the Vercel deploy synced the new function with Inngest; the Inngest dashboard is the only place that shows it.
- Measured on the production code path offline, claude-opus-5 judge: 12.5k output tokens, 127 to 133 s; adjudication as a patch 4k tokens, 41 to 45 s; writer 4.5k tokens, 55 s; one read $1.01 to $1.73 and 276 to 299 s, all in the background. The cached prefix (rules, vocabulary, tool schema) is byte-identical across companies; the warm judge call cost $0.39 against $0.81 cold.
- Four corpus cards produced reads through the production path: Cognition (2 running, 9 in question), August (1 running, 2 not yet, 7 in question), DatologyAI (3, 7), Friend (2, 5). Specialization is a running label on 3 of 4. The frequency gate (`strategyFrequencyGate`, max share 0.5, min 10 reads) was not reached: the fifteen-card batch stopped at four when the Anthropic key ran out of credit; credit has since been added.
- The critic-and-adjudication loop now lands every time and trims current labels with cited reasons (Cognition 4 to 2, August 4 to 1 in the same run). `HOW_IT_WINS_REFINEMENT=off` skips both, and the setting is part of the judgment cache key. Whether the trimmed read or the untrimmed one is the better product is Samay's blind read to make; the holdout ten (braintrust, mintlify, modretro, perplexity, plaud, proton, superhuman, superlist, uniqlo, vercel) are untouched and stay that way.
- Extension 0.2.8 is packaged at `dist/chrome-web-store/cold-start-chrome-0.2.8-c06d39fd65cf.zip` (SHA `31b6696fa47127d00c6e3db5338e43bf9c0d132b5f61d6dcc30af481077de06b`) in both trees and recorded in `docs/product/chrome-web-store-alpha/release-compatibility-matrix.md`. It has not been uploaded; 0.2.7 is superseded and 0.2.6 is still in review. The API contract is unchanged at `2026-08-19.how-it-wins-v1`. No alpha installation exists in production (zero rows), so the 0.2.5 lockout affects nobody through the alpha path.
- The auto-mode classifier blocked `git push` and `db:migrate:production` for the previous session; Samay ran both by hand. Expect the same for any push or production migration: prepare the exact command and hand it over rather than retrying.

## What to do, in order

1. **Prove the flag in production.** Ask Samay to run one analysis from his side panel on a company that already has a profile and to tell you the domain (the panel must carry the current contract: an unpacked `dist` from this tree, or 0.2.6 through 0.2.8). Then follow it: `generation_runs` for the parent run (status, `trace_json->'howItWins'`, `trace_json->'steps'->'how-it-wins'`), `research_run_events` for `how-it-wins.started` and `how-it-wins.complete`, `how_it_wins_judgments` for the stored verdict, and `cards.card_json->'synthesis'->'howItWins'` for the filed read. Report the run id, the judge call tokens, latency, and cost from `judgeSummary`, and whether the crown arrived in the panel. If `how-it-wins.started` fired but nothing followed within ten minutes, the Inngest sync is the first suspect: check `apps/web/src/app/api/inngest/route.ts` registers `howItWinsFunction`, and ask Samay to open the Inngest dashboard for `cold-start` and look for `how-it-wins-read`.
2. **Run `npm run measure:how-it-wins`** (self-loads the production env pair, read-only) and paste the status distribution. It must separate a `failed` pipeline from an honest `nothing_stands_out`. If it cannot classify the run you just watched, fix the script, not the run.
3. **Finish the frequency batch.** From the worktree, with `.env.local` sourced: `npm run eval:how-it-wins:batch -- --limit 15 --seed 825 --budget-usd 18 --parallel 2`. The four judged cards are cached on disk and cost nothing again. Report `strategyFrequencyGate` over the filed reads, per-card judge tokens, latency, and cost, and the loss counters. If specialization exceeds a 0.5 share across ten or more reads, the fix belongs in the judgment standard's specialization rubric row (the sitting-2 notes, finding 3, already name the habit), not in a display cap; write the proposed rubric line and stop for Samay's word before changing prompts.
4. **Prepare the refinement A/B without running it.** Write the two command lines (the same batch with `--no-refinement`, same seed, into its own run directory) and describe how the two run directories would be loaded into a blind sitting. Do not run it and do not touch `/eval/how-it-wins`.
5. **Close the loose ends**, each a small commit: re-apply the three lost doc lines to `CLAUDE.md` and `AGENTS.md` from the main checkout's uncommitted diff, then drop that stash; add `LLM_HOW_IT_WINS_JUDGE_MODEL` to `.env.example` and `docs/deployment.md` next to `HOW_IT_WINS_ENABLED`; add a retention rule for `how_it_wins_judgments` to `scripts/alpha-prune.ts` or the retention cron (rows older than 90 days, measured by `created_at`; the table grows one 60 to 80 KB row per distinct evidence packet); type `judgeSummary.refinement.repairs` if it is still untyped in `packages/core/src/generation-trace.ts`; note in `docs/anthropic-llm-call-map.md` that the judge's 1h cache write only pays back when a second call lands inside the hour (adjudication, a repair, or another company), which is the cost model to revisit if traffic stays sparse.
6. **Extension upload is Samay's.** After step 1 confirms the crown lands in the panel, tell him the 0.2.8 zip is ready and where; do not advance `release-version.json`.

## Decisions that stand

D1 to D12 from the original brief, plus these made on 2026-08-25: judge and writer both on claude-opus-5 until a blind read says otherwise; adjudication is a patch of disputed rows, never a re-emit; open-question rows are lean records; the evidence handle universe is 200 and constrained by one pattern; running floor 1, cap 6, in-question cap 12; verifier degrades only at zero survivors; the frozen writer never discards a verdict over an optional slot; contradictions in a verdict are repaired in code before any paid re-ask; refinement is switchable and is part of the cache key. Do not relitigate any of these. If evidence contradicts one, say so in the report and leave the call to Samay.

## Gates and limits

- Spend cap for this session: $25. The batch in step 3 is the bulk of it. Print the running total after every batch and stop at the cap.
- `npm run check` green by exit code before any commit you ask Samay to push. Read `echo $?`; never pipe check through `tail`.
- Do not read the holdout. Do not bump `packages/core/api-contract.json`.
- Rollback stays `HOW_IT_WINS_ENABLED=false` plus a redeploy (Vercel env changes only apply to a new deployment).
- Another session may be editing the main checkout; work in the worktree `~/Projects/active/cold-start-hiw` on the branch, and hand pushes to Samay.

## Final report

Plain sentences with numbers: the production run you can point at (id, cost, latency, what the crown shows); the measure script's distribution; the batch table and the gate verdict; what you spent; what is left for Samay; the one or two things you are least sure about.
