# Execution prompt: Investor Lens Overhaul, Phases 1.5 through 4

Saved 2026-07-21 for a fresh session. Paste as the opening message.

---

Execute: Investor Lens Overhaul, Phases 1.5 through 4

You are in /Users/samaydhawan/Projects/active/cold-start. Phase 1 of `docs/superpowers/plans/2026-07-20-investor-lens-overhaul.md` is shipped and deployed (merge `137d080`, contract `2026-07-20.synthesis-withheld-v1`). Execute the remaining phases IN SEQUENCE in this session: 1.5 (reference library), 2 (result surface and CSS truth), 3 (person hovercards), 4 (latency levers and the watchable wait). Carry each phase through its exit gate, ship it, then start the next. Do not stop between phases except at the checkpoints that name Samay.

Method: superpowers:subagent-driven-development, fresh implementer per task, task review after each, final whole-branch review per phase before its merge. The design authority is `docs/superpowers/specs/2026-07-20-investor-lens-overhaul-design.md`; where plan and spec disagree, the spec wins and you flag the conflict instead of guessing. The plan's WHERE WE LEFT OFF section is the position record; Phase 1's Done and Finding blocks are context worth skimming. The Phase 1 SDD ledger lives at `.superpowers/sdd/progress.md`; start a fresh ledger section for this run and work on a new branch per phase.

Read in order before any code: repo CLAUDE.md, the spec, the plan, DESIGN.md, docs/product/extension-motion-playbook.md. Re-grep every file:line anchor before editing; Phase 1 moved code.

Orchestration doctrine (~/.claude/FABLE-ORCHESTRATION.md): every subagent gets an explicit model and effort. Sonnet for implementation and tests, Opus for the taste-critical review of Phase 2 and 3 diffs and for each final whole-branch review, Haiku for mechanical sweeps. Packet prompts name the fable-execution and fable-verification skills. Keep session tokens for review verdicts and integration judgment, not file dumps.

Hard rules, unchanged from Phase 1:
- TDD per task; findings recorded inline in the plan as Finding and Done blocks; checkboxes flipped as you go.
- The synthesis brain is untouchable: taste kernel, verifier logic, synthesis model, prompt, max_tokens, and the two card-quality gates.
- `npm run check` green from the main tree gates every phase; never pipe check through tail.
- Prod DB access is read-only (`set -a; source .env.production.migrate.local; set +a`); SELECTs only.
- Contract bumps per plan; rebuild the extension on every bump; Firefox parity at the Phase 2 and 3 gates.
- No em-dashes in any copy; slopcheck every prose artifact (expect false positives on code negation operators in technical docs).

Stop and ask Samay only at: the Phase 2 gallery review (remind him: macOS Reduce Motion OFF; a frozen screen under reduced motion is a bug, not the design); the Task 5.3 promotion decision after the 20-run shadow table; the Phase 4 motion review; and anything that would touch issue #10's semantics (a gate-blocked re-run currently wipes a previously good read; his decision is pending, so do not build withheld-fixture behavior that assumes an answer).

Phase 1 lessons that bind this session:
- Compare content, not clocks. Any "unchanged since X" check that pits an assembly-time stamp against a persist-time stamp is unsatisfiable; the withheld pre-check shipped that bug once and the whole-branch review plus live QA caught it pre-merge. Hand final reviewers the cross-task seams explicitly.
- Prove RED honestly. The Phase 3 occlusion regression must demonstrably fail against pre-phase code before it counts; Task 4.3 spells out the checkout sequence.
- A parallel session may commit documentation onto your branch (blind-read capture notes). Leave those commits in place, never rebase the shared branch, and re-check git status before commits.
- If an implementer dies mid-task on infrastructure errors twice, dispatch a fresh finisher with a precise tree-state handoff instead of resuming a giant transcript a third time.
- review-package BASE is the commit recorded before the implementer started, never HEAD~1.
- Riding minors assigned to Phase 2, fold them in: TS exhaustiveness on the withheld reason-copy maps, the retry button's disabled affordance, and deriving the advisory label from the frozen record instead of live signals.

Phase-specific notes:
- 1.5: deep-research skill, three tracks in parallel; raw material stays gitignored in docs/motion-references/; the deliverable is `docs/product/gold-standard-references.md` and Phases 2 through 4 cite it.
- 2: the gallery harness lands first; the baseten card and deepinfra event stream come from read-only prod queries; CSS consolidates family by family, one commit each with screenshots; no @layer adoption this pass.
- 3: SharedTooltip has 8 call sites; the dock must never cover people rows, asserted by geometry in Playwright.
- 4: new Inngest step ids deploy in a quiet window with a repair:stuck-runs sweep after; re-fetch levers ship behind `ANALYSIS_SOURCE_REFRESH` defaulting to `full`; the two-week measurement tail follows the deploy, gate p50 at or under 60s and p90 at or under 90s via `npm run measure:analysis-latency` (locked baseline: wall p50 1m43s, p90 2m21s; dispatch right-skew already noted for Task 5.4).

Start now: Phase 1.5, Task 2.1. Report at each phase ship.
